import React from "react";

const Title = () => {
  return (
    <h1
      className="title"
      style={{
        textAlign: "center",
        marginTop: "30px",
        marginBottom: "10px",
        color: "gray",
      }}
    >
      Image Gallery
    </h1>
  );
};

export default Title;
